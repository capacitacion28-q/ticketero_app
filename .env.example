# ========================================
# SISTEMA TICKETERO - Variables de Entorno
# Generado el: 2025-12-24
# ========================================

# 🔴 VARIABLES CRÍTICAS (OBLIGATORIAS)
# ====================================

# Base de Datos PostgreSQL
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=ticketero_db
DATABASE_USER=ticketero_user
DATABASE_PASSWORD=change_me_in_production

# Telegram API
TELEGRAM_BOT_TOKEN=123456789:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_DEFAULT_CHAT_ID=5598409030
TELEGRAM_API_URL=https://api.telegram.org/bot
TELEGRAM_TIMEOUT=30000           # 30 segundos timeout
TELEGRAM_BOT_ENABLED=false       # Habilitar bot en producción

# 🟡 VARIABLES DE CONFIGURACIÓN (OPCIONALES)
# ==========================================

# Servidor de Aplicación
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# Schedulers (milisegundos)
SCHEDULER_MESSAGE_RATE=60000     # 60 segundos - procesamiento mensajes
SCHEDULER_QUEUE_RATE=5000        # 5 segundos - procesamiento colas
SCHEDULER_THREAD_POOL_SIZE=2     # Threads para schedulers

# Telegram API
TELEGRAM_TIMEOUT=30000           # 30 segundos timeout

# Sistema de Reintentos
RETRY_MAX_ATTEMPTS=3             # Máximo 3 reintentos
RETRY_INITIAL_DELAY=30000        # 30 segundos delay inicial
RETRY_MULTIPLIER=2.0             # Multiplicador backoff exponencial
RETRY_MAX_DELAY=120000           # 120 segundos delay máximo

# Auditoría y Compliance
AUDIT_RETENTION_DAYS=2555        # 7 años retención (2555 días)
AUDIT_BATCH_SIZE=1000            # Tamaño lote procesamiento

# 🟢 VARIABLES DE NEGOCIO (OPCIONALES)
# ====================================

# Configuración de Colas
NO_SHOW_TIMEOUT=5                # 5 minutos timeout no-show
MAX_CONCURRENT_TICKETS=3         # Máximo tickets por asesor

# ⚠️ NOTAS IMPORTANTES
# ====================
# 1. Las variables marcadas como OBLIGATORIAS deben configurarse
# 2. Variables OPCIONALES tienen valores por defecto seguros
# 3. En producción, cambiar todas las credenciales por defecto
# 4. El TELEGRAM_BOT_TOKEN debe obtenerse de @BotFather en Telegram